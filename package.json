{
  "name": "pos-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix next_pos\"",
    "build": "npm run build --prefix next_pos",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@zxing/browser": "^0.1.5"
  }
}